import { Template } from "./Template.js";
export declare class CheerioTemplate implements Template {
    private $;
    constructor(html: string);
    getIslands(): {
        island: string;
        json: string;
    }[];
    removeScripts(test: {
        src?: RegExp;
        text?: RegExp;
    }): void;
    removeModulePreloadLinks(): void;
    insertMarkup(markup: Record<string, string>): void;
    toString(): string;
}
