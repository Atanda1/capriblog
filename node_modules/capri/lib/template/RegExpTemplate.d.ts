import { Template } from "./Template.js";
export declare class RegExpTemplate implements Template {
    private html;
    constructor(html: string);
    getIslands(): {
        island: string;
        json: string;
    }[];
    removeScripts(test: {
        src?: RegExp;
        text?: RegExp;
    }): void;
    removeModulePreloadLinks(): void;
    insertMarkup(markup: Record<string, string>): void;
    toString(): string;
}
