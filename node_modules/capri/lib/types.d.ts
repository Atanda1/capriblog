export declare type Markup = Record<string, string | Promise<string>>;
export declare type RenderResult = Markup | null | undefined;
export declare type RenderContext = {
    getHeader(name: string): string | null;
    setHeader(name: string, value: string): void;
};
export declare type RenderFunction = (url: string, context: RenderContext) => RenderResult | Promise<RenderResult>;
export interface IslandOptions {
    media?: string;
}
