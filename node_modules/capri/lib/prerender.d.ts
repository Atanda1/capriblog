export declare type PrerenderConfig = false | string | string[] | (() => string[] | Promise<string[]>);
export declare type FollowLinksConfig = boolean | ((pathname: string) => boolean);
declare type StaticRenderConfig = {
    ssrBundle: string;
    createIndexFiles: boolean;
    outDir: string;
    base: string;
    prerender: PrerenderConfig;
    followLinks: FollowLinksConfig;
};
export declare function renderStaticPages({ ssrBundle, createIndexFiles, outDir, base, prerender, followLinks, }: StaticRenderConfig): Promise<string[]>;
export declare function urlToFileName(url: string, extraDir: boolean, base: string): string;
export {};
