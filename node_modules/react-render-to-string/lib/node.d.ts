/// <reference types="node" />
import { EventEmitter } from "events";
import { ReactNode } from "react";
export default function renderToString(element: ReactNode): Promise<string>;
/**
 * Naive WriteableStream implementation that captures chunks in memory
 * ands invokes a callback with the concatenated result as utf8 string
 * once the end of the stream is signaled.
 */
export declare class StreamReader extends EventEmitter implements NodeJS.WritableStream {
    private cb;
    writable: boolean;
    private chunks;
    constructor(cb: (s: string) => void);
    write(chunk: Uint8Array | string): boolean;
    end(): this;
}
